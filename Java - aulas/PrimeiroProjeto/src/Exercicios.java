import java.util.Scanner;

public class Exercicios {
    public static void main(String[] args) {
        //Fahrenheit pra celsius
       /* Scanner input = new Scanner(System.in);

        System.out.print("Digite a temperatura em Fahrenheit: ");
        double temperatura = input.nextDouble();

        double celsius = (temperatura - 32) / 1.8;
        System.out.println("Temperatura em Celsius é: " + celsius);

        input.close();

        //Celsius para fahrenheit
        Scanner input = new Scanner(System.in);

        System.out.print("Digite a temperatura em Celsius: ");
        double temperatura = input.nextDouble();

        double fahrenheit = (temperatura * 1.8) + 32;
        System.out.println("Temperatura em Celsius é: " + fahrenheit);

        input.close();

        //IMC
        Scanner input = new Scanner(System.in);

        System.out.println("Digite sua altura(M): ");
        double altura = input.nextDouble();

        System.out.println("Digite seu peso(KG): ");
        double peso = input.nextDouble();

        double IMC = peso / Math.pow(altura, 2);
        System.out.println(IMC);

        input.close();

        //Valor ao quadrado
        Scanner input = new Scanner(System.in);

        System.out.println("Digite o valor desejado: ");
        double valor= input.nextDouble();

        double quadrado = Math.pow(valor, 2);
        System.out.println(quadrado);

        double cubo = Math.pow(valor, 3);
        System.out.println(cubo);

        input.close();

        //Triângulo
        Scanner input = new Scanner(System.in);

        System.out.println("Digite a base do triângulo: ");
        double base = input.nextDouble();

        System.out.println("Digite a altura do triângulo: ");
        double altura = input.nextDouble();

        double area = (base * altura)/2;
        System.out.println(area);

        input.close();

        //Equação
        int a = 1;
        int b = 12;
        int c = -13;

        double delta = Math.pow(b, 2) - 4 * a * c;
        System.out.println("o valor de delta é: " + delta);

        //Desafio
        double a = -3;
        double b = -1;
        double x = 3 * Math.pow(a/4, -2) + 6 * (Math.pow(3, b) / 4) -4;
        double y = 7 * Math.pow(a/4, b) + 2;
        double z = Math.pow((x/y), b) + 4;
        System.out.print(z);*/


    }
}

